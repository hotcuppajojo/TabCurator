{
  "scripts": {
    "build-chrome": "node build-scripts/build-chrome.js",
    "build-firefox": "node build-scripts/build-firefox.js",
    "build-safari": "node build-scripts/build-safari.js",
    "package-chrome-release": "zip -r build/chrome-extension.zip dist/",
    "package-firefox-release": "zip -r build/firefox-extension.zip dist/",
    "package-safari-release": "zip -r build/safari-extension.zip dist/",
    "test": "jest --config jest.config.js"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "playwright": "^1.48.2"
  },
  "dependencies": {
    "@playwright/test": "^1.48.2",
    "fs-extra": "^11.2.0",
    "terser": "^5.36.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
